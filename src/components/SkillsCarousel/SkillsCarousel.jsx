import React from 'react';
import styles from './SkillsCarousel.module.scss';

const SkillsCarousel = () => {
  return (
    <div className={`${styles.monitor} ${styles.center}`}>
      <p>
        HTML CSS SCSS JAVASCRIPT REACT BOOTSTRAP JSON REST BEM GIT TYPESCRIPT
        SQL ES6 REACT ROUTER
      </p>
    </div>
  );
};

export default SkillsCarousel;
